import 'dart:ui';

class Mytheme {
static const primaryPurple = Color(0xFF534AB7);      // header, button, icon
static const lightPurple = Color(0xFFEEEDFE);        // icon bg, category card bg
static const purpleTitle = Color(0xFFEEEDFE);        // header title text
static const purpleSubtitle = Color(0xFFAFA9EC);     // header subtitle text
static const darkPurple = Color(0xFF3C3489);         // category card text
static const tealBg = Color(0xFFE1F5EE);             // difficulty card bg
static const tealDark = Color(0xFF085041);           // difficulty card text
static const tealMid = Color(0xFF0F6E56);            // difficulty card icon
static const statsBg = Color(0xFFF5F5F5);            // stats row bg

//********************* Quiz Screen  ******************************* */
static const quizBg = Color(0xFF0F0F1A);          // Scaffold background
static const cardBg = Color(0xFF1E1E35);           // question card, option cards
static const optionUnselected = Color(0xFF2A2A4E); // option circle bg when not selected
static const correctGreen = Color(0xFF1D9E75);     // correct option border, icon, circle
static const wrongRed = Color(0xFFE24B4A);         // wrong option border, icon, circle
static const pointsGold = Color(0xFFFAC775);       // points text and trophy icon
static const timerGreen = Color(0xFF5DCAA5);       // timer text and clock icon
static const progressActive = Color(0xFF534AB7);   // filled progress bar segment
static const optionTextDim = Color(0xFFAFA9EC);    // unselected option text after answer
}